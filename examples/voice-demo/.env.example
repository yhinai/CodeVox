# OpenAI API Key (Required for all examples)
# Get your key at: https://platform.openai.com/account/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# Server Configuration
PORT=8085
HOST=localhost

# HTTPS Configuration (required for microphone access on non-localhost)
# Set to 'true' to enable HTTPS with self-signed certificates
# Required when accessing from IP addresses or remote hosts
USE_HTTPS=false
